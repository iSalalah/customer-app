# حالة بوابة الخدمات الذاتية — بلدية ظفار

آخر تحديث: 2026-09-05

## ما هو هذا النظام

بوابة خدمات ذاتية للمواطنين تعمل على **شاشات لمس عامة (Kiosk)** داخل مباني
البلدية، ومعها **لوحة تحكّم للموظفين** على الويب.

المواطن يسجّل الدخول برقم هاتفه ورمز تحقق، يقدّم طلبًا على خدمة بلدية، يرفق
مستنداته، يتابع حالته، ويردّ حين تطلب البلدية معلومات إضافية. وأي شخص يستطيع
الاستعلام عن حالة طلب برقم المرجع دون تسجيل دخول.

الموظف يعمل على قائمة الطلبات ضمن نطاق دوره: **مدير الدائرة** يرى دائرته،
**رئيس القسم** يرى قسمه، و**الموظف** يرى ما أُسند إليه وحده.

> لا علاقة له بـ**مرصاد ظفار الاجتماعي** ولا بـ**مرصاد المختبرات** رغم أن
> الثلاثة تخصّ ظفار. جذر git مستقل، ولا كود مشترك.

## البنية

مستودع أحادي (monorepo) بمساحات عمل npm. **جافاسكربت خالصة — لا TypeScript
إطلاقًا**، ولا مترجم TS في أي خطوة بناء.

| المسار | ما فيه |
|---|---|
| `apps/api` | واجهة Express 4 + Prisma 5 + MySQL 8 + Redis 7 |
| `apps/kiosk` | تطبيق المواطن على الشاشة اللمسية (React + Vite) |
| `apps/admin` | لوحة الموظفين (React + Vite) |
| `packages/shared` | مصفوفة الحالات والأدوار والحدود وقواعد الملفات — مصدر واحد للحقيقة |
| `docker` | ملفات Docker وnginx وتهيئة MySQL |
| `docs` | عشر وثائق: الافتراضات، المعمارية، الصلاحيات، سير العمل، قاعدة البيانات، الواجهة، التهديدات، النشر، قائمة التحقق |

كل وحدة في الواجهة الخلفية من خمسة ملفات ثابتة: `routes` / `controller` /
`service` / `repository` / `schemas`. لا منطق أعمال في المتحكّمات، ولا وصول
لـPrisma من أي خدمة داخل `src/modules`.

## المتطلبات الثلاثة التي شكّلت التصميم

### 1. الشاشة العامة مشتركة بين الناس

جلسة المواطن تنتهي بعد **دقيقتين من عدم النشاط**، والتنفيذ **على الخادم** لا
على مؤقّت في المتصفّح. عند الانتهاء تُلغى الجلسة في Redis وفي MySQL معًا،
وتُمسح حالة الواجهة وذاكرة الاستعلامات، ويعود الجهاز إلى الشاشة العامة.

لا يُكتب أي شيء حسّاس في `localStorage` — لا رمز جلسة ولا بيانات مواطن ولا
مسودّة طلب. وكل استجابة مصادَق عليها تحمل `no-store`، وزرّ الرجوع في المتصفّح
لا يُظهر شاشة المواطن السابق.

**اختُبر فعليًا في متصفّح حقيقي:** تُرك الجهاز بلا لمس، فانتهت الجلسة وحدها،
وعاد إلى الرئيسية بإشعار «انتهت جلستك وتم مسح بياناتك من هذه الشاشة»،
و`localStorage` و`sessionStorage` فارغان، والخادم يرفض الكعكة القديمة بـ401.

### 2. التوجيه ليس قرار المتصفّح

حين يختار المواطن خدمة، تُقرأ الدائرة والقسم **من سجلّ الخدمة في قاعدة
البيانات**. أي `departmentId` أو `sectionId` في جسم الطلب يُرفض بـ400 عبر
`.strict()` في Zod — لا يُتجاهل بصمت.

### 3. إخفاء زرّ ليس ضابط أمان

كل قرار صلاحية يُعاد اشتقاقه على الخادم من **السجلّ المُحمَّل نفسه**، في طبقة
الخدمة، عند كل نداء. حرّاس المسارات طبقة أولى خشنة فقط. الواجهة تُخفي الأزرار
لراحة الموظف لا كضابط، ولا تُستشار أبدًا.

الطلب خارج نطاق الموظف يُجاب بـ**404 لا 403** — لأن تأكيد وجود المعرّف نفسه
معلومة.

## الأمان

| الجانب | ما هو مطبَّق |
|---|---|
| الجلسات | رموز معتمة 256 بت في كعكات `HttpOnly`/`Secure`/`SameSite=Strict`؛ لا يُخزَّن إلا `sha256` منها |
| رمز التحقق | ستّ خانات من `crypto.randomInt`، يُخزَّن كـHMAC-SHA256 بفلفل بيئي، صلاحية 5 دقائق، 5 محاولات، تهدئة 60 ثانية، وسقف لكل رقم ولكل IP |
| كلمات الموظفين | Argon2id (m=19456, t=2, p=1)، قفل 15 دقيقة بعد 5 محاولات، وتدوير رمز التجديد مع كشف إعادة الاستخدام |
| المرفقات | PDF/JPEG/PNG فقط، تحقّق من **البصمة الثنائية** ومن النوع المعلن معًا، 10 ميغا للملف و5 ملفات للطلب، مفاتيح تخزين UUID خارج أي جذر ويب، وكل تنزيل يمرّ بمتحكّم يفحص الصلاحية |
| التتبّع العام | أربعة حقول فقط، و`APPROVED` و`REJECTED` يظهران كـ`CLOSED` معًا حتى لا تُستخرج النتائج بالتعداد |
| الأخطاء | لا أثر مكدّس ولا رسالة SQL ولا Prisma في أي استجابة؛ التفصيل يُسجَّل مرة واحدة تحت معرّف ارتباط يُعاد للعميل |

### ما ليس مطبَّقًا — بصراحة

- **لا مصادقة ثنائية للموظفين.** هذا أهم بند مؤجَّل، وموثَّق في `docs/07-security.md` تحت `T9`.
- **فحص الفيروسات معطَّل في التطوير.** الإنتاج يرفض الإقلاع بدونه (`MALWARE_SCAN_ENABLED=true` إلزامي).
- **الرقم المدني لا يُجمع في هذه النسخة.** الأعمدة موجودة ومشفّرة (AES-256-GCM) لاستيراد لاحق من الخلفية — السبب في `docs/01-assumptions.md` بند `B5`.
- **الطلبات المغلقة لا تُعاد فتحها.** `APPROVED` و`REJECTED` نهائيتان، ولا يوجد مسار لإعادة الفتح.
- **لا اختبار حمل ولا اختبار اختراق.** حدود المعدّل مُستنتَجة بالمنطق لا مقيسة على حركة حقيقية.

## ما اختُبر فعلًا

| الفحص | النتيجة |
|---|---|
| `npm test` على MySQL وRedis حقيقيين | **234 اختبارًا، 15 مجموعة، صفر إخفاق** |
| `npm run lint` | صفر أخطاء وصفر تحذيرات |
| `npm audit --omit=dev` | **صفر high وصفر critical** |
| `prisma migrate deploy` على MySQL 8.4 | طُبِّقت الهجرة، و`migrate dev` يقول «متطابق» |
| اختبار دخاني عبر HTTP حيّ | **37 فحصًا، كلها ناجحة** |
| تشغيل التطبيقين في متصفّح حقيقي | الدخول، تبديل العربية/الإنجليزية، لوحة المفاتيح الافتراضية، انتهاء الجلسة، نطاق الصلاحيات |
| الحزمة كاملة على Docker | الخدمات الخمس سليمة، و`ready=200`، وترويسات الأمان الخمس على كل المسارات |
| **استنساخ نظيف من GitHub** | `npm install` ثم `lint` و`build` و234 اختبارًا — كلها ناجحة بلا أي خطوة يدوية |
| **التكامل المستمر على GitHub** | أخضر: تدقيق، اختبار على MySQL وRedis حقيقيين، بناء، تدقيق تبعيات، وحارسا أسرار |

### ثلاثة أخطاء إضافية ظهرت عند تجهيز المستودع للتسليم

7. **ترويسات الأمان غائبة تمامًا من حاويات الواجهة** رغم أنها معرَّفة في إعداد
   nginx. السبب قاعدة توريث: `add_header` لا يُورَّث إلى أي `location` يعرّف
   `add_header` خاصًا به، وكتلتا `= /index.html` و`/assets/` تعرّفان
   `Cache-Control` فأسقطتا كل ترويسات الأمان **بصمت**. نُقلت إلى
   `docker/nginx-security-headers.conf` ويُستدعى بـ`include` داخل كل كتلة.
8. **تسع مجموعات اختبار تفشل على استنساخ نظيف** بدل أن تُتخطّى: عميل Prisma
   غير مولَّد بعد `npm install`. أُضيف `postinstall`. لم يظهر إلا باستنساخ
   المستودع من الصفر واتّباع README حرفيًا.
9. **تصفير قاعدة الاختبار لم يكن يعطّل القيود فعلًا.** `SET FOREIGN_KEY_CHECKS`
   متغيّر جلسة، وPrisma ينفّذ عبر مجمّع اتصالات، فقد يقع `TRUNCATE` على اتصال
   آخر. مرّ محليًا بالمصادفة وفشل على CI. أُلغي تعطيل القيود واعتُمد ترتيب
   الحذف داخل `$transaction`، مع فحص يرفض أي جدول جديد غير مُدرَج.

### ستة أخطاء حقيقية ظهرت بالتشغيل وأُصلحت

1. **تغيير الحالة كان يطلب ملاحظة دائمًا** — `.default()` في Zod جعل شرط
   «الملاحظة مطلوبة» يعمل في كل مرة.
2. **ملف مشوَّه كان يُرجع 500 بدل 415** — `file-type` *يرمي* استثناءً على حاوية
   مبتورة، وهو بالضبط ما يرسله المهاجم. صار جدول البصمات **تحقّقًا متقاطعًا**
   يجب أن يوافق الحاوية، لا احتياطيًا قد يُنقذها.
3. **أول إجراء من متصفّح جديد كان يُرفض دائمًا** — لا توجد كعكة CSRF بعد، لأن
   الشاشة الرئيسية لا تنادي الواجهة أصلًا. لم يظهر إلا بتشغيل الواجهة الحقيقية.
4. حالة حدود المعدّل كانت تتسرّب بين الاختبارات (مخزن في الذاكرة).
5. استجابة 429 للتتبّع كانت بلا ترويسات منع التخزين.
6. `prisma migrate dev` كان يفشل على تهيئة نظيفة (صلاحية قاعدة الظل).

## التشغيل محليًا

```bash
cp .env.example .env      # ثم املأ الأسرار الأربعة
npm install
docker compose up -d mysql redis
npm run prisma:deploy
npm run prisma:seed
npm run dev
```

- شاشة المواطن: <http://localhost:5173>
- لوحة الموظفين: <http://localhost:5174>
- مستكشف الواجهة: <http://localhost:4000/api/docs>

حسابات الموظفين للتطوير يطبعها سكربت البذور، وكلمتها جميعًا `Dhofar#Dev2026`.
**لا توجد في الإنتاج** — السكربت يرفض العمل إذا كان `NODE_ENV=production`.

المواطنون يُنشأون عند أول تحقّق ناجح. في التطوير يُطبع رمز التحقق في طرفية
الواجهة الخلفية مسبوقًا بـ`[DEV SMS]`، ولا يُطبع في أي بيئة أخرى ولا يُعاد في
أي استجابة.

## الخطوة التالية المقترحة

1. مصادقة ثنائية لحسابات الموظفين.
2. اختبار حمل على حدود المعدّل بحركة تشبه ازدحام الصباح.
3. ربط مزوّد رسائل قصيرة حقيقي وتجربة التسليم فعليًا.
